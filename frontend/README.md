# AI Text-to-Video Generator — Frontend

## Setup

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

The dev server starts at http://localhost:3000.

Make sure the backend is running at http://localhost:8000.
